import { FC } from 'react'
import { rgbColor } from '@/types/color'

interface IndexProps {
  value: rgbColor
}

const Swatch: FC<IndexProps> = ({}) => {
  return <div></div>
}

export default Swatch
